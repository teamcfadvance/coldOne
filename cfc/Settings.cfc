<cfcomponent hint="Provides settings for interacting with Open Beer DB">
	<cfset this.baseURL = 'http://api.openbeerdatabase.com/v1' />
    <cfset this.publicToken = '[YOUR PUBLIC TOKEN HERE]' />
    <cfset this.privateToken = '[YOUR PRIVATE TOKEN HERE]' />
</cfcomponent>